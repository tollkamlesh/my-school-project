// Example Rust code to demonstrate basic struct and tuple usage.
struct Point {
    x: f64,
    y: f64,
}

fn main() {
    let point1 = Point { x: 3.0, y: 2.0 };
    println!("Point1: ({}, {})", point1.x, point1.y);
}
